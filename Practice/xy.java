package Practice;
import java.util.*;
public class xy {
    public static void main(String[] args) {
        //long a=Long.parseUnsignedLong("999999999999999999");
                                       //999999999999999999
        //long b=999999999999999999;//9888888888888888880
        //System.out.println(a);
        /*
        int a=7^1^7^1^7^1^99;
        int  b=7&1&7&1&7&1&99;
        int c=7|1|7|1|7|1|99;
        //System.out.println(a+" "+b+" "+c);

        long aa=25;
        double temp1=Math.log10(aa);
        double temp2=Math.log10(2);
        int digits=(int)(Math.ceil(temp1/temp2));
        System.out.println(digits+" "+(temp1/temp2));
        
        int a=7;
        int digits=(int)(Math.ceil((Math.log10(a))/Math.log10(2)));
        int num=(int)Math.pow(2, digits)-1;
        System.out.println(num^a);
        
        int arr[] = {0,1,2,3,4,5,6,7,8};
        Arrays.sort(arr);
        //Arrays.sort(arr, (Integer a,Integer b)->(a>b?1:0));
        //Arrays.sort(arr,(a,b)->(a.bits==b.bits)?a.val-b.val:a.bits-b.bits);

        for(int i=0; i<arr.length; i++)
        System.out.print(arr[i]+" ");

        Integer[] arr1 = Arrays.stream(arr) // IntStream
								.boxed()				// Stream<Integer>
								.toArray(Integer[]::new);
        int xss[]=func(arr1);
    }
    static int[] func(Integer[] arr)
    {
        return arr;
    }
    public static Integer count(Integer a)
    {
        int cnt=0;
        while(a!=0)
        {
            ++cnt;
            a=a&(a-1);
        }
        return cnt;
    }*/
}
}